{
  "name": "facebook-mcp-sse",
  "version": "1.0.0",
  "description": "Facebook MCP Server with SSE transport — deployable to Railway, Render, Fly.io",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
